```
# Build and run with Docker Compose (with volume)
python webapp/train_model.py
docker compose up --build -d

# Open
# http://localhost:8000

# Stop and remove
# docker compose down -v
```


